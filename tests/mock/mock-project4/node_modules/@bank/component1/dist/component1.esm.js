import { useBrand } from '@bank/core';
import { jsx } from '@emotion/core';

/** @jsx jsx */
function Component1(_ref) {
  var _ref$look = _ref.look,
      look = _ref$look === void 0 ? 'look1' : _ref$look,
      children = _ref.children;

  var _useBrand = useBrand(),
      COLORS = _useBrand.COLORS;

  var styleMap = {
    look1: COLORS.color1,
    look2: COLORS.color2,
    look3: COLORS.color3
  };
  return jsx("div", {
    css: {
      label: "alert-".concat(look),
      background: styleMap[look]
    }
  }, children);
}

export { Component1 };
