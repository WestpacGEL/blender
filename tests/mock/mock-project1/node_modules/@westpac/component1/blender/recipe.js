import React, { Fragment } from 'react';
import { Core, useBrand } from '@westpac/core';

import { Component1 } from '@westpac/component1';

export function AllStyles({ brand }) {
	const { COLORS } = useBrand();

	return (
		<Core brand={brand}>
			<Component1 look={COLORS.color1}/>
			<Component1 look='blue'/>
			<Component1 look='green'/>
		</Core>
	);
}

export function Docs({ brand }) {
	return (
		<Core brand={brand}>
			<h2>Component 1</h2>

			<Component1 look='red'/>
		</Core>
	);
}
