'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var core = require('@emotion/core');
var react = require('react');

/** @jsx jsx */
var BrandContext = react.createContext();
var useBrand = function useBrand() {
  var brandObject = react.useContext(BrandContext);
  var errorMessage = "You need to wrap your components in the Core component.";

  if (!brandObject) {
    throw new Error(errorMessage);
  }

  return brandObject;
};
function Core(_ref) {
  var brand = _ref.brand,
      children = _ref.children;
  return core.jsx(BrandContext.Provider, {
    value: brand
  }, core.jsx("div", {
    css: {
      label: "core",
      background: 'rebeccapurple',
      ':after': {
        content: '"THIS IS CORE!!!!"'
      }
    }
  }, children));
}

exports.Core = Core;
exports.useBrand = useBrand;
