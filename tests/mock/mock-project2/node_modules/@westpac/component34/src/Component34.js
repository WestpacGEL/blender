/** @jsx jsx */

import { useBrand } from '@westpac/core';
import { jsx } from '@emotion/react';

export function Component34({ look = 'look1', children }) {
	const { COLORS } = useBrand();

	const styleMap = {
		look1: COLORS.color1,
		look2: COLORS.color2,
		look3: COLORS.color3,
	};

	return (
		<div
			css={{
				label: `component34-${look}`,
				background: styleMap[look],
			}}
		>
			{children}
		</div>
	);
}
