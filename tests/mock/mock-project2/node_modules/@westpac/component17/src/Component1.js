/** @jsx jsx */

import { jsx } from '@emotion/core';

export function Component1({ look = '', children }) {
	return (
		<div
			css={{
				label: `alert-${look}`,
				background: look,
			}}
		>
			{children}
		</div>
	);
}
