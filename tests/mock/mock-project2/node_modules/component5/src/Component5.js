/** @jsx jsx */

import { jsx } from '@emotion/core';

export function Component5({ look = '', children }) {
	return (
		<div
			css={{
				label: `alert-${look}`,
				background: look,
			}}
		>
			{children}
		</div>
	);
}
