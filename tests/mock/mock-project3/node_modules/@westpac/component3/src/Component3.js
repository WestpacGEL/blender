/** @jsx jsx */

import { jsx } from '@emotion/core';

export function Component3({ look = '', children }) {
	return (
		<div
			css={{
				label: 'alert-foo',
				background: look,
			}}
		>
			{children}
		</div>
	);
}
