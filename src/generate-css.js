/**
 * All functions to generate css files
 *
 * generateCSSFile - TODO
 **/

function generateCSSFile(css) {
	// ids loop
	// generate class names
	// replace prefix in css

	return css;
}

module.exports = exports = {
	generateCSSFile,
};
